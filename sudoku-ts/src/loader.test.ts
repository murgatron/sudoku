/* eslint-disable no-undef */
describe('puzzle loader', () => {
  it('can load arg --four', () => {
    expect(true).toBe(true);
  });
  it('can load arg --nine', () => {
    expect(true).toBe(true);
  });
  it('can load arg --twelve', () => {
    expect(true).toBe(true);
  });
  it('can load arg --sixteen', () => {
    expect(true).toBe(true);
  });
  it('can load a custom --puzzle', () => {
    expect(true).toBe(true);
  });
  it.skip('can load a random matrix', () => {
    expect(true).toBe(true);
  });
});
